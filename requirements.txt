numpy>=1.26
opencv-python>=4.9
pillow>=10.0
matplotlib>=3.8

# PyTorch (CPU). For CUDA, install from https://pytorch.org/ instead.
torch>=2.2
torchvision>=0.17

# Segment Anything (Meta). Installed from GitHub for the latest version.
segment-anything @ git+https://github.com/facebookresearch/segment-anything.git

# Mineral classification — local CLIP zero-shot backend (default).
open_clip_torch>=2.24

# Dashboard / UI
streamlit>=1.30
pandas>=2.0
plotly>=5.18

# Optional: Claude-vision classifier backend (`--backend claude`).
# Needs ANTHROPIC_API_KEY. Uncomment to install.
# anthropic>=0.40
